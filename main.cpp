#include "types_and_keywords.hpp"
#include "tokenizing.hpp"
#include "parser.hpp"
#include "writeToFiles.hpp"

int main()
{
    parsing();
    // for(int i = 0; i < allvars.size(); ++i)
    // {
    //     std::cout << allvars[i].var_type << " " << allvars[i].name <<
    //     " " << " = " << allvars[i].val;
    //     std::cout << std::endl;
    // }
    // std::cout << std::endl;
}